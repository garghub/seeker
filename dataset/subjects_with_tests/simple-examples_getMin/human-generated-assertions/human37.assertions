assert(ch.usi.gassert.util.Implies.implies(a <= b, result == a));
assert(ch.usi.gassert.util.Implies.implies(a > b, result == b));
assert( (ch.usi.gassert.util.Implies.implies(a <= b, result == a) && ch.usi.gassert.util.Implies.implies(a > b, result == b)));
