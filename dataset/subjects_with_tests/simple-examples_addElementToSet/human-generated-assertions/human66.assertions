assert((intSet == null) || (ch.usi.gassert.util.Implies.implies(!old_set.contains(element), old_intSet.size() == intSet.size() + 1 && old_element == element) && ch.usi.gassert.util.Implies.implies(old_set.contains(element), old_intSet.size() == intSet.size() && old_element == element)));
