assert(intSet.contains(element));
assert(((intSet.size() == old_intSet.size()) && old_intSet.contains(element)) || ((intSet.size() == (old_intSet.size() + 1)) && old_intSet.contains(element)));
assert( (intSet.contains(element) && ((intSet.size() == old_intSet.size()   && old_intSet.contains(element)) || (intSet.size() == old_intSet.size()+1 && !old_intSet.contains(element)))));
