assert(old_intSet.size() == (intSet.size() + 1));
assert(old_element == element);
assert(old_intSet != intSet);
assert( (old_intSet.size() == intSet.size() + 1 && old_element == element && old_intSet != intSet));
