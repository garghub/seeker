assert((intSet.size() == (old_intSet.size() + 1)) || (intSet.size() == old_intSet.size()));
assert(intSet.contains(element));
assert((intSet.size() == old_intSet.size() + 1 || intSet.size() == old_intSet.size()) && intSet.contains(element));
