assert(ch.usi.gassert.util.Implies.implies(old_intSet.contains(old_element), old_intSet.size() == intSet.size()));
assert(ch.usi.gassert.util.Implies.implies(!old_intSet.contains(old_element), old_intSet.size() == intSet.size() - 1));
assert(old_element == element);
assert( (ch.usi.gassert.util.Implies.implies(old_intSet.contains(old_element), old_intSet.size() == intSet.size()) && ch.usi.gassert.util.Implies.implies(!old_intSet.contains(old_element), old_intSet.size() == intSet.size() - 1) && old_element == element));
