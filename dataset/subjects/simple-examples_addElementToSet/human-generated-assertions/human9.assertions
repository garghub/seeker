assert(intSet.size() == (old_intSet.size() + 1));
assert((old_element == element) || intSet.contains(element));
assert( (intSet.size() == old_intSet.size() + 1 && (old_element == element || (intSet.contains(element)))));
