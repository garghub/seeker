assert(old_intSet.size() > 0);
assert(intSet.size() > 1);
assert(old_intSet.size() == (intSet.size() - 1));
assert(old_element == element);
assert( (old_intSet.size() > 0 && intSet.size() > 1 && old_intSet.size() == intSet.size() - 1 && old_element == element));
