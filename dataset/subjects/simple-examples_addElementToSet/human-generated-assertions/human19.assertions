assert(old_intSet.size() == (intSet.size() + 1));
assert(old_element == element);
assert(old_element != null);
assert( (old_intSet.size() == intSet.size() + 1 && old_element == element && old_element != null));
